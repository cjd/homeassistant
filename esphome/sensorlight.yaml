esphome:
  name: sensorlight
  platform: ESP8266
  board: esp01_1m
  
wifi:
  ssid: virus
  password: !secret wifi_password

api:

logger:

ota:

binary_sensor:
  - platform: gpio
    pin:
      number: GPIO0
      mode: INPUT_PULLUP
      inverted: True
    name: "Sensorlight Button"
    on_press:
      - light.toggle: light
  - platform: gpio
    pin: GPIO2
    name: "PIR Sensor"
    device_class: motion
    
light:
  - platform: binary
    name: "Sensor Light"
    id: light
    output: relay
    
output:
  - platform: gpio
    pin: GPIO12
    id: relay
    inverted: no

status_led:
  pin:
    number: GPIO13
    inverted: yes